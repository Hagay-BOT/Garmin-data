# משימות ל-Opus (effort=high) — מהסקירה המעמיקה 15.07.2026

> **כללי-עבודה לכל משימה (חובה, לפני הכל):**
> 1. `git pull` → קרא `SESSION_STATE.md` + הרלוונטי מ-`ENGINEERING_DECISIONS.md`.
> 2. קוד חדש = עם בדיקות. **"בוצע" = מאומת בפועל**, לא "אמור לעבוד".
> 3. כל משלוח דרך `python ship.py "הודעה" [קבצים]` — מריץ קומפילציה+כל הטסטים+commit+push.
> 4. שינוי התנהגות-משתמש או דחיפה לגרמין — רק באישור מפורש של חגי.
> 5. בסיום: עדכן `SESSION_STATE.md` + `CHANGELOG_AI.md`. כשל → תקן את **המחלקה** + הוסף guard.

---

## ✅ T1 (בוצע 24.07) · מחולל: אובדן-נפח שקט + תקרת ריצה-קלה
**הבעיה:** ב-`plan_generator.py` (שורות ~113-119), אם ימי-הקל עמוסים — הק"מ הנותר נעלם
בלי שום דיווח; ואם נשאר יום-קל אחד, הוא עלול לקבל 9+ ק"מ (כבר לא "קלה").
**מה לעשות:** (1) אחרי פיזור הקל — אם `סך-בפועל < target_km*0.9`, הוסף ל-notes של התוכנית
שורת "⚠️ נפח בפועל X מתוך Y — ימים עמוסים" (מגיעה לטלגרם דרך הדוח). (2) תקרת ריצה-קלה
8 ק"מ: עודף מעבר לה עובר ליום-קל השני אם קיים, אחרת מדווח כנ"ל.
**אימות:** הוסף 2 מקרים ל-`test_plan_generator.py` (כל הימים-הקלים עמוסים → דיווח; יום-קל
יחיד עם עודף → תקרה 8). כל הטסטים ירוקים דרך ship.

## ✅ T2 (בוצע 24.07) · פרסור העדפות — לא רק אילוצים
**הבעיה:** `parse_availability` ב-`plan_generator.py` מזהה רק ימים-עמוסים. "מעדיף לונג
בשישי" נבלע — התשובה של חגי לא מזיזה כלום.
**מה לעשות:** הרחב את הפרסור לזהות `מעדיף/עדיף + (לונג|איכות|ריצה) + ב<יום>` →
`{'prefer': {'long': weekday}}`. ב-`generate()`: העדפת-לונג גוברת על ברירת-המחדל שבת
(אם היום המועדף פנוי); אותו דבר לאיכות. שמרני: ביטוי לא-מזוהה = מתעלמים (הטקסט הגולמי
ממילא מגיע לנרטיב של ה-LLM).
**אימות:** 3 מקרים חדשים ב-`test_plan_generator.py`: "מעדיף לונג בשישי" מזיז לונג לשישי;
העדפה ליום-עמוס לא חלה; טקסט בלי העדפה לא משנה כלום.

## ✅ T3 (בוצע 24.07) · אימות סקאלת ה-feel מול גרמין
**הבעיה:** בריצת 12.07 חגי דירג "קלה מאוד" אבל נשמר `feel=3` (בינוני, raw=50). ייתכן באג-
סקאלה כמו ה-RPE (שתוקן: raw10=קל-מאוד) או לחיצה על סמיילי אמצעי.
**מה לעשות:** ב-`fetch_garmin.py` (~שורות 256-283, 370): הוסף ל-debug print את **כל** שדות
ה-summaryDTO שמכילים feel/rpe/exert בריצה האחרונה. בקש מחגי לדרג בריצה הבאה ערך קיצון
("קל מאוד") והשווה raw↔תצוגה. אם יש היסט — תקן את המיפוי `{0:1,25:2,...}` + הוסף בדיקת-גבול
כמו של ה-RPE. **אל תנחש** — תקן רק מול נתון אמיתי.

## ✅ T4 (בוצע 24.07) · איחוד מסלול-הכוח (הכפילות מה-audit)
**הבעיה:** שני מסלולים דוחפים כוח לגרמין: `push_week` (גנרי "💪 כוח A" ממשבצת-זמן/DB) ו-
`strength-schedule`/`push_strength` (מפורט "משיכה — גב..."). ה-audit ב-13.07 הראה שבפועל
בשעון נשארים המפורטים, והגנרים שנדחפו נעלמים/מוחלפים. מבלבל ושביר.
**מה לעשות:** קבע מסלול אחד — **המפורט מנצח**. `push_week` ידחוף ריצות בלבד; אימוני-כוח
של התוכנית יידחפו דרך אותו builder מפורט (`push_strength.build_strength_workout_full`)
שכבר קיים כ-fallback ב-`build_strength`. ודא ש-`garmin_client.OUR_MARKERS` מכסה את כל
השמות. עדכן את `strength-schedule.yml` בהתאם (ואם מתייתר — סמן, אל תמחק בלי חגי).
**אימות:** `push-week.yml -f mode=audit` לפני/אחרי — אפס כפילויות, כל פריט מסומן "שלנו",
כוח אחד בלבד ליום.

## 🟡 T5 (קוד בוצע — ממתין לאישור-נוסח) · שער-מוכנות לאימוני-איכות (הלב של BUILD #2 / M3.2 — מאושר ע"י חגי)
**הרקע:** ב-13.07 חגי ישן 3.5 שעות ביום-סף — **הוא** תפס את זה, לא המערכת, למרות שנתוני
השינה קיימים ב-`data.json` (daily: sleep_score/hours, body battery).
**מה לעשות:** פונקציה דטרמיניסטית `readiness_gate(metrics, today_session)` (בלי LLM):
אם היום אימון quality/long **וגם** (שינה < 5ש' או sleep_score < 50 או body battery נמוך
קיצוני) → שלח הודעת טלגרם קצרה: "היום מתוכנן <שם>; השינה (Xש'/ציון Y) לא תומכת — מציע
להחליף לקל/מנוחה. השב 'החלף' או התעלם." תשובת "החלף" מטופלת ע"י לולאת-העריכה הקיימת
(`weekly_revise` דרך `telegram_intake`). שלב ב-watcher הקיים (`coach-postworkout.yml` רץ
כל שעה) — **לא** workflow חדש. ספי-ההחלטה = קבועים ב-`athlete.py`.
**אימות:** בדיקות-יחידה על הגייט (שינה טובה→שקט; קצרה+יום-איכות→הודעה; קצרה+יום-קל→שקט).
זהו שינוי התנהגות — קבל אישור-נוסח מחגי על ההודעה לפני resync ראשון.

## ✅ T6 (בוצע 24.07) · זיכרון-מצב-גוף מתמשך (athlete_state)
**הבעיה:** "כאב ברך" ביומן נשכח אחרי שבוע; אין דגלים מתמשכים.
**מה לעשות:** `athlete_state.json` (דרך `store.py`: load/save) עם דגלים פעילים
`{"knee": {"since": ..., "note": ...}}`. פתיחה/סגירה בהודעת-טלגרם טבעית ("הברך שוב כואבת" /
"הברך בסדר") — זיהוי מילות-מפתח דטרמיניסטי ב-`telegram_intake` (קטגוריה חדשה `body_flag`),
בלי LLM. הדגלים מוזרקים לפרומטים (שבועי+פוסט) ול-safety `factors` (כבר תומך ב-injury).
**אימות:** בדיקות intake לקטגוריה החדשה; דגל פתוח מופיע בפרומט; safety מקבל factor.

## 🎨 T7 · דשבורד: רצועת "מתוכנן ↔ בוצע" + הערות-יומן
**מה לעשות:** ב-`index.html`: (1) בכרטיס-השבוע — לכל יום הצג את המתוכנן (מ-`week_plan.json`,
כבר נטען?) מול מה שבוצע (מ-`data.json`), עם ✓/✗/≈. (2) סקשן קטן "הערות אחרונות" מ-
`journal.json` (5 אחרונות). **עיצוב:** להתאים לשפה הקיימת של הדשבורד (dark, כרטיסים).
שים לב: אם `journal.json`/`week_plan.json` לא נטענים היום ב-JS — הוסף fetch עם fallback
שקט. עוגן-שבוע: השתמש ב-`weekBounds` הקיים (ראשון!). **אימות:** פתח את הדשבורד לוקאלית,
צלם, ודא רספונסיבי במובייל (רוב השימוש של חגי מהטלפון).

## 📚 T8 · פיצול ה-KB (M5.2) — core מול per-loop
**הבעיה:** כל קריאת-LLM מזריקה ~20k טוקן של KB מלא; הפוסט-אימון לא צריך את מחקר-התחרויות.
**מה לעשות:** ב-`load_knowledge_base` הוסף פרמטר `mode` — core (פרופיל+כללים+משפחות-מדדים,
לכולם) + תוספות per-mode (weekly: מחקר/periodization; postworkout: רק ניתוח-ריצה).
חלוקה לפי הקבצים הקיימים ב-KB_DIR (בדוק `KB_PRIORITY`). **בלי לשכתב תוכן** — רק ניתוב.
**אימות:** מדוד גודל-פרומט לפני/אחרי לכל מצב (צפי: פוסט ~50%-); בדיקת רינדור לשני המצבים.

## 🔧 T9 · M7 — איחוד ה-workflows (המפה כבר ב-ENGINEERING_BACKLOG.md)
לפי המפה שנרשמה (4 קולטני-טלגרם → פולר אחד עם `telegram_intake`; הסרת cron הבוקר המת
מ-coach-postworkout; מסלולי-דחיפה → אחרי T4). **גדול — לבצע רק אחרי T4-T5, בסשן ייעודי.**
כלל-ברזל: אסור שינוי התנהגות — אותם זמנים, אותן תגובות. אימות: יום מלא של לוגים תקינים.

## 👀 T10 · בדיקת שער ה-Time-Trial (שבוע 4 במאקרו)
**מה לעשות:** בדוק ב-`macro_plan.json` שקיים gate של 5K TT סביב שבוע 4 (הוחלט בעבר:
כיול VDOT לפני קפיצת-יעד). אם חסר — הוסף לשבוע המתאים (`gate: "5K TT"`) וודא
ש-`get_macro_week` + השבועי מציפים אותו כשמגיעים. **אל תשנה יעדי-נפח** — רק הוספת ה-gate.

---
**סדר מומלץ:** T1→T2 (זוג קטן, אותו קובץ) · T3 (מהיר) · T5 (הערך הגדול) · T4 · T6 · T7 · T8 · T10 · T9 בסוף.
