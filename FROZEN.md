# ❄️ הפרויקט מוקפא — 26.07.2026

הוקפא לבקשת הגיא (תקופה עמוסה). **אפס אוטומציות, אפס הודעות, אפס עלות API.**

## מה הוקפא
כל 16 ה-workflows ב-GitHub Actions מושבתים (`disabled_manually`), כולל:
משיכת נתוני גרמין · Data Watcher · סיכום שבועי · שאלת-זמינות · לולאת-עריכה ·
דוח-בריאות/heartbeat · deploy · יומן/מזג-אוויר · כל מסלולי הדחיפה לגרמין.

**לא נמחק שום דבר.** הקוד, הנתונים, התוכניות וההיסטוריה שלמים.

## ⚠️ פעולה אחת שנשארה לך (מחוץ ל-repo)
**cron-job.org** — הטריגר החיצוני שהגדרת שם ממשיך לנסות לדפוק ל-GitHub.
ה-workflows מושבתים אז כלום לא ירוץ, אבל **ייתכן שתקבל מיילי-כשל**.
👉 היכנס ל-cron-job.org והשהה (pause) את הג'וב. זה הדבר היחיד שאני לא יכול לעשות.

## איך מפשירים (2 דקות)
```bash
cd "C:\Users\03hag\Claude projects\garmin-dashboard"
for w in update deploy coach-postworkout coach-weekly coach-weekly-revision \
         ask-availability coach-evening-reply health-heartbeat calendar-refresh \
         ci-tests approve-week push-week adhoc-workout backfill-splits \
         strength-schedule test-workout; do gh workflow enable "$w.yml"; done
```
או פשוט תגיד לי **"תפשיר"** ואני אעשה הכל.

### אחרי ההפשרה — 3 דברים לבדוק
1. **הפעל מחדש את cron-job.org.**
2. **`macro_plan.json` יהיה מיושן** — התוכנית בנויה לתחרות 20.09.2026. אם עברו שבועות,
   צריך לכייל מחדש את המאקרו (שבוע נוכחי/יעדי נפח) לפני הסיכום השבועי הראשון.
3. **דגלי-גוף פעילים** ב-`athlete_state.json`: מחלה + ברכיים + כף רגל (מ-25-26.07).
   אם עבר לך — תכתוב בטלגרם "עבר לי" או שאסגור ידנית, אחרת התוכנית תיבנה מוקלת מדי.

### GitHub מכבה cron אחרי 60 יום ללא פעילות
זה כבר קרה בפועל (workflows מושבתים) — אז זה לא משנה כלום עכשיו.
פשוט זכור: אחרי ההפשרה, ודא שהריצות באמת מתחילות לרוץ (`gh run list`).

## מצב הפרויקט ברגע ההקפאה
- **13 milestones הנדסיים סגורים ומאומתים.** CI ירוק על כל commit.
- **המשימות שנשארו** מתועדות ב-`OPUS_TASKS.md` (T9 — איחוד workflows) וב-
  `ENGINEERING_BACKLOG.md`. `SESSION_STATE.md` מחזיק את הצעד המדויק הבא.
- **הנושא הפתוח האחרון:** הגיא ביקש לבטל את ניתוח-האימונים (בוצע) ולהעלות במקומו
  את הנתונים המעניינים לדשבורד — **בחירת הפרמטרים לא הושלמה**. רשימת 10 המועמדים
  נמצאת בשיחה; להתחיל משם כשחוזרים.
